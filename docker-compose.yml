services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: hanet
      POSTGRES_PASSWORD: hanet
      POSTGRES_DB: hanet
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
