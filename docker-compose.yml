services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: hanet
      POSTGRES_PASSWORD: hanet
      POSTGRES_DB: hanet
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
